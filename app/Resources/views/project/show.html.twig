{% extends 'base.html.twig' %}

{% block body %}
    <h1>{{ project.name }}</h1>

    <h3>Descripción</h3>
    <p>{{ project.description }}</p>
    <p>{{ project.image }}</p>

    <ul>
        <li>
            <a href="{{ path('projects_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
